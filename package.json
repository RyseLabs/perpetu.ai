{
  "name": "perpetu-ai",
  "version": "0.1.0",
  "private": true,
  "description": "AI Tabletop RPG Maker",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "postinstall": "echo 'Dependencies installed. Run: npm run dev or pnpm dev'"
  },
  "packageManager": "pnpm@8.15.0",
  "engines": {
    "node": ">=18.0.0"
  }
}
